 # 环境安装
 torch 是一个庞大且依赖复杂的包，建议不要用 pip 安装，而是使用 Conda，从 PyTorch 官方镜像直接拉取：
```
 conda install pytorch==1.13.0 torchvision==0.14.0 numpy=1.24.3 gym==0.26.1 -c pytorch
```

## 安装可视化依赖包
```
conda install matplotlib seaborn numpy
```


 # 命令
 python generate_test.py RADAR_X PredatorPrey-8 0.5
 python training_run.py RADAR PredatorPrey-8 0.5

 # 新命令
 <!-- 基于belief + VDN  -->
 python generate_test.py BELIEF_RADAR PredatorPrey-8
 <!-- 基于belief + QMIX -->
 python training_run.py BELIEF_QMIX PredatorPrey-8

 # 文件说明
 ## 1.分支说明
 -  radar 源代码 正常运行
 -  add_belief 分支用来添加belief信念机制 改蹦了
 -  radar_belief 分支用来添加belief信念机制  第二种修改思路
 -  belief_observations_rewards 目标： 实现收敛，基于历史信息去实现；
## 2. 关键文件功能说明
各个文件的功能说明
1. belief_qmix.py — Belief-QMIX学习器实现
这是系统的主学习模块，集成了以下功能：
- 信念建模（Belief Network）：估计每个智能体是否是对抗性的，输出belief_probs信念概率。
- 历史追踪：追踪每个智能体的观察、动作、奖励序列，用于生成更准确的信念。
- 信念引导的更新机制：信念高的（可能是对抗的）智能体会被赋予低权重。
- 训练主网络与目标网络（qmix & target_qmix）：进行Q值更新和信念网络更新。
- 在线识别对抗者：动态根据belief_probs识别adversary。
- 策略执行 & 更新策略：结合Q值和信念值决策动作，同时分析每个智能体的行为贡献。
与QMixNet的接口：
- 在self.qmix_net和self.target_qmix_net中调用QMixNet结构；
- 传入agent_qs, state, beliefs来获得混合后的q_total。

2. mixing.py — QMixNet 网络结构
这是一个混合网络（Mixing Network），将多个智能体的Q值整合为一个全局Q值，适用于集中式训练分布式执行的多智能体场景。
- 超网络（Hypernetwork）机制：根据全局状态动态生成权重和偏置；
- 双层注意力机制：
  - 状态注意力（state attention）：依据全局状态调节各智能体的权重；
  - 信念注意力（belief attention）：依据信念值识别可疑智能体；
  - 防御性融合机制（defensive_weights）：1 - belief，加权削弱对抗性agent的影响；
  - 贡献度分析（Contribution Analysis）：打印出每个agent在Q值合成中的贡献度和对抗倾向；
- 前向传播forward：输出混合后的q_total，供训练使用。
3. belief.py — 信念网络模块（Belief Network）
这是一个模块化的神经网络，专门用于评估每个 agent 的可信度（是否是对抗者）。
| 模块                         | 功能说明 |
|------------------------------|----------|
| `Belief` 类                  | 整体信念网络结构，负责前向传播、信念值输出、更新策略等 |
| `forward()`                  | 输入每个 agent 的历史观测、动作等 → 输出信念值 |
| `update_policy()`            | 基于行为（如变化频率）和信念值 → 判断是否需要更新策略 |
| `analyze_agent_contributions()` | 输出每个 agent 的对抗性得分、行为活跃度、贡献比重等 |
| `normalize_input()`          | 将观测值归一化，避免异常数据影响模型 |
| `RNNLayer` + `MLP/CNNBase`   | 模型内部网络结构支持序列数据建模（RNN）或视觉输入建模（CNN） |
| `training_stats`             | 包含训练动态指标，如最佳回报、平均信念、无改善轮数等 |

4. adversarial_agent.py — 对抗性（拜占庭）智能体实现与管理
该模块用于定义和管理多种类型的对抗性智能体（如拜占庭、随机、反向、自私、噪声、懒惰等），支持灵活分配和行为模拟。
- 对抗类型枚举：AdversarialType，涵盖多种对抗/异常行为模式。
- AdversarialAgent 类：实现不同对抗策略的动作采集（如最大化损失、扰动、反向、拜占庭等），支持历史行为统计与特征提取。
- AdversarialAgentManager 类：支持在实验配置中灵活指定/随机分配对抗智能体，统一管理、动作分发、奖励处理与统计分析。
- create_adversarial_config 辅助函数：支持按比例、策略（如随机、混合、单一）自动生成对抗智能体配置。
- 典型用法：在每个 episode 开始时，随机/指定部分 agent 为对抗性，采集动作时优先判断是否为对抗 agent 并调用特殊策略。
- 支持行为特征输出，便于信念网络识别和训练。
